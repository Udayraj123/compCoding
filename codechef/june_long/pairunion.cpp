#include<bits/stdc++.h>
#define ll long long int
#define MAX 100000
#define printa(x,n) for(int i = 0; i < n; ++i)printf("%d ", x[i]);printf("\n");
#define printla(x,n) for(int i = 0; i < n; ++i)printf("%lld ", x[i]);printf("\n");
using namespace std;
/*
So the numbers are repeating between sets, but not within.
Given N sets of different numbers from 1 to K.
Out : no of pairs whose union makes k.

M1 : Check for presence of the compliment
	>> a mark array of size k <= 2500 to denote set 
	>> 
*/
int main(int argc, char const *argv[])
{
	ll t=100,t1,n=1,k=1;
	cin>>t; 
	while(t--){
		cin>>n>>k;
		std::vector<ll> t;
		for (int i = 0; i < n; ++i)
		{
			cin>>leni;
			for (int j = 0; j < leni; ++j)
			{
				cin>>t1;
				// set[j]
			}
		}

		cout<<(negs+hp*p)<<endl;
	}
	return 0;
}

